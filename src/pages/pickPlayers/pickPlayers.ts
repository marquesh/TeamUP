import { Component } from "@angular/core";
import { NavController } from "ionic-angular";

@Component({
  selector: "page-pickPlayers",
  templateUrl: "pickPlayers.html"
})
export class PickPlayers {
  constructor(public navCtrl: NavController) {}
}
